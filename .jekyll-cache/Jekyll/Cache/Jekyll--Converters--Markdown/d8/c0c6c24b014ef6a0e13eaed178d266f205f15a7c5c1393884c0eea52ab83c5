I" <h2 id="목차">목차</h2>
<ul>
  <li><a href="#nimf">nimf 란?</a></li>
  <li><a href="#bug">끝글자 버그</a></li>
  <li>설치하기
    <ul>
      <li><a href="#install">nimf 설치</a></li>
      <li><a href="#config">nimf 환경설정</a></li>
      <li><a href="#deact">nimf 타언어 비활성화</a></li>
    </ul>
  </li>
  <li><a href="#procon">장단점</a></li>
  <li><a href="#ref">참조</a></li>
</ul>

<hr />

<p><br /></p>

<h2 id="nimf-란-">nimf 란? <a id="nimf"></a></h2>
<p>nimf는 참다참다 폭발한 일반인이 프로그래밍을 배워 개발한 프로그램으로 
타 입력기(ibus, fcitx, etc)에 존재하는 <strong>끝글자 버그</strong>를 고친 한글 입력기이다.</p>

<p><br /></p>
<h2 id="끝글자-버그-">끝글자 버그 <a id="bug"></a></h2>
<p>Elementary OS 0.4.1 Loki에서의 한글 입력이 불편한 이유는 공백(스페이스) 입력 때문이다. <br />
예를들어 <code class="highlighter-rouge">한글 입력기</code>를 입력하려고 하면 아래와 같은 상황을 마주보게 된다.</p>

<blockquote>
  <p><code class="highlighter-rouge">한글 입력기</code> –&gt; <code class="highlighter-rouge">한 글입력기</code></p>
</blockquote>

<p>보다시피 공백이 끝글자(<code class="highlighter-rouge">글</code>)의 앞이 아닌 뒤에 출력되는데, 이걸 끝글자 버그라고 한다.</p>

<p><br /></p>
<h2 id="nimf-설치-">nimf 설치 <a id="install"></a></h2>

<p>터미널을 열고 아래의 명령어를 실행해 nimf를 설치한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> nimf
</code></pre></div></div>

<p><br /></p>
<h2 id="nimf-환경설정-">nimf 환경설정 <a id="config"></a></h2>

<p>입력 방법을 nimf로 설정한다.</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>im-config <span class="nt">-n</span> nimf
</code></pre></div></div>

<p>위 과정을 완료한 후 로그아웃하고 다시 로그인하면 nimf를 이용한 한글 입력이 가능해진다.</p>

<p><br /></p>
<h2 id="nimf-타언어-비활성화-">nimf 타언어 비활성화 <a id="deact"></a></h2>

<p>nimf는 기본적으로 일본어와 중국어의 입력도 활성화되어 있다. <br />
이를 비활성화 하기 위해서는 nimf 설정 창을 열고</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>nimf-setting
</code></pre></div></div>

<p>anthy와 sunpinyin를 비활성해주면 된다.</p>

<p><br /></p>
<h2 id="장단점-">장단점 <a id="procon"></a></h2>
<p>한국어 입력에는 정말 만족한다. 공백을 입력할 때 마다 방향키로 한 칸 옮겨주고 공백을 입력했는데
그럴 필요가 없어졌으니 상당히 편하다.</p>

<p>다만 문제는 일본어 입력에 있다. 애초에 한글 입력에 편의성을 위해서 제작된걸로 알기 때문에 이걸
단점이라고 봐야할지는 모르겠으나, 일본어 또한 nimf의 기능 중 하나이니 개인적인 불만을 토로해본다.</p>

<p>일본어 입력이 불편한 이유는 자동으로 ん(n)이 완성되지 않는다. 
예를들어 べんきょう를  입력하면 べnきょう가 되어버린다. ん을 사용하려면 일일이 nn를 입력해야 하는데 
이는 일본어 입력기로는 절대적으로 불편한 부분이다.</p>

<p>개발자의 블로그를 보니 처음에는 ん의 자동완성 기능을 제공했는데, 어느 날 갑자기 이 기능을 
빼버린 것 같다. 그러면 이 때 아예 일본어 입력을 nimf에서 빼는게 낫지 않았을까. 
이 외에도 카나에서 한자로 변환할 때 스페이스바 대신 방향키를 이용해야 하는 불편함이 있다.</p>

<p>결론:  <br />
한국어만 사용한다면 nimf를 쓰자.</p>

<p><br /></p>
<h2 id="참조-">참조 <a id="ref"></a></h2>
<ul>
  <li><a href="https://elementaryos.stackexchange.com/questions/8041/adding-korean-input-for-loki">StackExchange : Adding Korean Input</a></li>
  <li><a href="https://cogniti-works.blogspot.com/2018/09/nimf-23.html#comment-form">nimf 개발자 블로그</a></li>
</ul>
:ET